{
  "name": "root",
  "private": true,
  "scripts": {
    "react-tally-ui:no-browser": "cross-env BROWSER=none npm run react-tally-ui",
    "react-tally-ui": "lerna run start --scope=react-tally-ui",
    "electron-app:after:react-tally-ui": "wait-on tcp:3000 && npm run electron-app",
    "electron-app": "lerna run start --scope=electron-tally",
    "start": "concurrently -k \"npm run react-tally-ui:no-browser\" \"npm run electron-app:after:react-tally-ui\" ",
    "build": "lerna run build --scope=react-tally-ui && lerna run build --scope=electron-tally"
  },
  "workspaces": {
    "packages": [
      "packages/apps/*"
    ],
    "nohoist": [
      "electron",
      "electron/**",
      "electron-squirrel-startup",
      "electron-squirrel-startup/**",
      "@electron-forge",
      "@electron-forge/**"
    ]
  },
  "devDependencies": {
    "concurrently": "^7.6.0",
    "cross-env": "^7.0.3",
    "lerna": "^6.4.1",
    "wait-on": "^7.0.1"
  }
}
